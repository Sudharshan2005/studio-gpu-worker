# GPU worker deps for vai-dev2 (driver 580.173.02, CUDA 13.0).
# Driver 580 is new enough to run cu124 OR cu128 torch wheels (drivers are
# backward-compatible with older CUDA runtimes). cu128 is the closest match;
# fall back to cu124 if a wheel is missing. Confirm on-box with:
#   python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
--extra-index-url https://download.pytorch.org/whl/cu128
torch
torchvision
torchaudio
diffusers>=0.35.0        # Wan 2.2 support (WanPipeline / WanImageToVideoPipeline)
transformers>=4.49.0
accelerate>=1.0.0
ftfy
imageio
imageio-ffmpeg
soundfile
kokoro>=0.9.4            # Kokoro TTS
huggingface_hub>=0.26.0
requests>=2.32.0
# ACE-Step + LatentSync install from source in bootstrap (git repos, not on PyPI)
