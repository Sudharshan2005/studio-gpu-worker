# GPU worker deps for vai-dev2 (driver 580.173.02, CUDA 13.0).
# Install from PyPI ONLY -- it ships cu130 builds, an exact match for this box.
# Do NOT add an --extra-index-url for a cuXXX wheelhouse: with two eligible
# indexes pip resolves per-package and splits the stack (torch+torchvision came
# back cu130 from PyPI, torchaudio came back +cu128), and torchaudio then raises
# at import on the CUDA mismatch -- surfacing as a bogus "Could not import
# UMT5EncoderModel" from the transformers lazy loader. Confirm on-box with:
#   python -c "import torch, torchaudio; print(torch.__version__, torchaudio.__version__, torch.cuda.is_available())"
torch
torchvision
torchaudio
torchcodec              # torchaudio 2.9+ save() delegates to TorchCodec's encoder; ACE-Step's .wav write needs it
diffusers>=0.35.0        # Wan 2.2 support (WanPipeline / WanImageToVideoPipeline)
transformers>=4.49.0
accelerate>=1.0.0
ftfy
imageio
imageio-ffmpeg
soundfile
kokoro>=0.9.4            # Kokoro TTS
huggingface_hub>=0.26.0
requests>=2.32.0
# ACE-Step + LatentSync install from source in bootstrap (git repos, not on PyPI)
