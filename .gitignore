# Live credentials. .env is NOT committed -- it holds a real WORKER_TOKEN and
# HF_TOKEN. Copy .env.example to .env and fill it in locally.
.env
# local backups of edited scripts (can contain secrets copied out of .env)
*.orig
.venv/
__pycache__/
*.pyc
outputs/
LatentSync/
loras/*.safetensors
*.mp4
*.wav
*.png
